import{NextRequest,NextResponse}from"next/server";import{authorizeRequest}from"@/lib/auth/session";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{submitActivity}from"@/lib/core/records";export async function POST(r:NextRequest,c:{params:Promise<{activityId:string}>}){try{const p=await authorizeRequest(r,"activity.self.manage");const{activityId}=await c.params;return NextResponse.json({success:true,activity:await submitActivity(p.tenantId,p.accountId,activityId)})}catch(e){return evaluationErrorResponse(e)}}
