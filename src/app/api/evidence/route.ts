import{NextRequest,NextResponse}from"next/server";import{authorizeRequest}from"@/lib/auth/session";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{storeEvidenceUpload}from"@/lib/evaluation/evidence-storage";
export const runtime="nodejs";export async function POST(r:NextRequest){try{const p=await authorizeRequest(r,"appraisal.evaluate");const form=await r.formData();const file=form.get("file");if(!(file instanceof File))return NextResponse.json({success:false,error:"One evidence file is required"},{status:400});return NextResponse.json({success:true,evidence:await storeEvidenceUpload(p.tenantId,p.accountId,file)},{status:201})}catch(e){return evaluationErrorResponse(e)}}
