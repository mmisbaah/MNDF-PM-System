import{NextRequest,NextResponse}from"next/server";import{authenticateRequest}from"@/lib/auth/session";import{requireAdministratorAuthorization}from"@/lib/auth/administrator-authorization";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{loadWorkspace}from"@/lib/core/workspace";
export const runtime="nodejs";export async function GET(r:NextRequest){try{const p=await authenticateRequest(r);await requireAdministratorAuthorization(p);return NextResponse.json({success:true,workspace:await loadWorkspace(p)},{headers:{"Cache-Control":"private, no-store"}})}catch(e){return evaluationErrorResponse(e)}}
