import{NextRequest,NextResponse}from"next/server";import{authorizeRequest}from"@/lib/auth/session";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{createCorrectionVersion}from"@/lib/corrections/service";
export const runtime="nodejs";export async function POST(r:NextRequest){try{const p=await authorizeRequest(r,"appraisal.evaluate");const b=await r.json() as{authorizationId:string;reason:string};return NextResponse.json({success:true,correction:await createCorrectionVersion(p.tenantId,p.accountId,b.authorizationId,b.reason??"")},{status:201});}catch(e){return evaluationErrorResponse(e);}}
