import{NextRequest,NextResponse}from"next/server";import{authorizeRequest}from"@/lib/auth/session";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{flagAdministrativeError}from"@/lib/corrections/service";
export const runtime="nodejs";export async function POST(r:NextRequest,c:{params:Promise<{appraisalId:string}>}){try{const p=await authorizeRequest(r,"personnel.manage");const{appraisalId}=await c.params;const b=await r.json()as{reason?:string};return NextResponse.json({success:true,flag:await flagAdministrativeError(p.tenantId,p.accountId,appraisalId,b.reason??"")},{status:201});}catch(e){return evaluationErrorResponse(e);}}
