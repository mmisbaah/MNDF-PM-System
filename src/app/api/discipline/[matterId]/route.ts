import{NextRequest,NextResponse}from"next/server";import{authorizeRequest}from"@/lib/auth/session";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{updateDiscipline}from"@/lib/core/records";export async function PATCH(r:NextRequest,c:{params:Promise<{matterId:string}>}){try{const p=await authorizeRequest(r,"conduct.manage");const{matterId}=await c.params;const b=await r.json();return NextResponse.json({success:true,matter:await updateDiscipline(p.tenantId,p.accountId,matterId,b.status)})}catch(e){return evaluationErrorResponse(e)}}
