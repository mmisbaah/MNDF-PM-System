import{NextRequest,NextResponse}from"next/server";import{authorizeRequest,authenticateRequest}from"@/lib/auth/session";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{getTrainingMode,setTrainingMode}from"@/lib/reports/service";
export const runtime="nodejs";export async function POST(r:NextRequest){try{const p=await authorizeRequest(r,"template.manage");const b=await r.json()as{enabled?:boolean};return NextResponse.json({success:true,settings:await setTrainingMode(p.tenantId,p.accountId,b.enabled===true)});}catch(e){return evaluationErrorResponse(e);}}
export async function GET(r:NextRequest){try{const p=await authenticateRequest(r);return NextResponse.json({success:true,activeDataMode:await getTrainingMode(p.tenantId,p.accountId)});}catch(e){return evaluationErrorResponse(e);}}
