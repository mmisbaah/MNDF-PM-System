import{NextRequest,NextResponse}from"next/server";import{authorizeRequest}from"@/lib/auth/session";import{evaluationErrorResponse}from"@/lib/evaluation/http";import{exportQuarterlyPdf}from"@/lib/reports/service";
export const runtime="nodejs";export async function GET(r:NextRequest,c:{params:Promise<{appraisalId:string}>}){try{const p=await authorizeRequest(r,"report.read");const{appraisalId}=await c.params;const pdf=await exportQuarterlyPdf(p.tenantId,p.accountId,appraisalId);return new NextResponse(new Uint8Array(pdf),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename=quarterly-${appraisalId}.pdf`,"Cache-Control":"no-store, private"}});}catch(e){return evaluationErrorResponse(e);}}
